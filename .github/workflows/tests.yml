name: Python Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      # Don't cancel all matrix jobs if one fails
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install MPI and system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y mpich libmpich-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov coverage mpi4py

      # Required for coverage to start in MPI subprocesses.
      # coverage needs a sitecustomize.py hook to activate in spawned processes.
      - name: Configure coverage for subprocess support
        run: |
          python -m coverage --version
          # Write .coveragerc
          cat > .coveragerc << 'EOF'
          [run]
          parallel = true
          source = fiesta
          concurrency = multiprocessing

          [report]
          show_missing = true
          EOF

          # Install the coverage sitecustomize hook so MPI subprocesses
          # automatically start coverage when spawned
          SITE_PACKAGES=$(python -c "import site; print(site.getsitepackages()[0])")
          cat > "$SITE_PACKAGES/sitecustomize.py" << 'EOF'
          import coverage
          coverage.process_startup()
          EOF

      - name: Run tests with coverage
        env:
          COVERAGE_PROCESS_START: .coveragerc
        run: |
          coverage erase

          # Run standard tests
          coverage run --parallel-mode -m pytest tests/normal_tests

          # Run MPI tests — --oversubscribe allows >1 rank per core on CI runners
          # -x makes mpirun propagate non-zero exit codes from any rank
          mpirun -n 2 --oversubscribe \
            -x COVERAGE_PROCESS_START \
            coverage run --parallel-mode -m pytest -s -v tests/mpi_tests

          # Merge all .coverage.* files produced by parallel/MPI runs
          coverage combine

      - name: Generate coverage report
        # Always generate even if tests had failures, so we can diagnose
        if: always()
        run: |
          coverage report -m
          coverage xml -o coverage.xml

      - name: Save coverage to summary
        if: always()
        run: |
          echo "## Coverage Report (Python ${{ matrix.python-version }})" >> $GITHUB_STEP_SUMMARY
          coverage report -m >> $GITHUB_STEP_SUMMARY

      # Only upload to Codecov once, from the latest Python version, on success
      - name: Upload coverage reports to Codecov
        if: success() && matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          slug: knaidoo29/FIESTA
